<% if @flat.photos.size > 1 %>
  <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
    <div class="carousel-inner">
      <div class="carousel-item active">
        <%= cl_image_tag @flat.photos.first.key, :class=>"d-block w-100", :alt=>"Flat image", :height=>450, :width=>1200, :crop=>"scale" %>
      </div>
      <% @flat.photos.drop(1).each do |photo| %>
        <div class="carousel-item">
          <%= cl_image_tag photo.key, :class=>"d-block w-100", :alt=>"Flat image", :height=>450, :width=>1200, :crop=>"scale" %>
        </div>
      <% end %>
    </div>
    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
      <span class="carousel-control-prev-icon" aria-hidden="true"></span>
      <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
      <span class="carousel-control-next-icon" aria-hidden="true"></span>
      <span class="sr-only">Next</span>
    </a>
  </div>
<% else %>
  <%= cl_image_tag @flat.photos.first.key, :class=>"img-fluid flat-photo", :alt=>"Flat image" %>
<% end %>
